{
  "name": "meshboard",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "check:local": "tsc -p tsconfig.check.json --noEmit",
    "check:version": "node scripts/check-version.mjs",
    "check": "npm run check:version && tsc -p tsconfig.check.json --noEmit"
  },
  "dependencies": {
    "@meshtastic/sdk": "^1.0.0",
    "@meshtastic/transport-web-serial": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^7.0.0",
    "vite": "^8.0.0"
  }
}
